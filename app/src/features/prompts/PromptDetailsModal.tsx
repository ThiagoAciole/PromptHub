import { Button, Code, Drawer, Group, Stack, Text, Title } from "@mantine/core";
import { IconCode } from "@tabler/icons-react";
import type { Prompt } from "../../lib/api.types";
export function PromptDetailsModal({ prompt, opened, onClose, onCopy }: { prompt: Prompt | null; opened: boolean; onClose: () => void; onCopy: (prompt: Prompt) => void }) { return <Drawer opened={opened} onClose={onClose} position="right" size="min(50vw, 680px)" title={prompt ? <Group><div className="detail-icon"><IconCode size={22} /></div><Title order={2}>{prompt.title}</Title></Group> : undefined} classNames={{ content: "detail-drawer", header: "detail-drawer-header", body: "detail-drawer-body" }}>{prompt && <Stack><Text size="sm" c="dimmed">{prompt.language} · {prompt.type}</Text><div className="detail-meta"><Text size="sm">Tags</Text><Group gap="xs">{prompt.tags.map((tag) => <span className="detail-tag" key={tag}>{tag}</span>)}</Group></div><Title order={3}>Prompt Content</Title><Code block>{prompt.content}</Code><Group justify="flex-end"><Button variant="subtle" onClick={onClose}>Fechar</Button><Button onClick={() => onCopy(prompt)}>Copiar prompt</Button></Group></Stack>}</Drawer>; }
